const colors = {
  blue: '#0f4c81',
  white: '#f5f5f5',
  black: '#2f3435',
  green: '#81cc45',
  skyblue: '#a0b8d0',
};

const radius = '10px';
const radiusSmall = '5px';

const theme = {
  ...colors,
  radius,
  radiusSmall,
};

export default theme;
